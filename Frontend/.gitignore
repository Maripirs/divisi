node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright (e2e). The saved login session and all run artifacts stay local;
# the config + specs + helpers are committed.
/e2e/.auth
/test-results
/playwright-report
/blob-report
/playwright/.cache

# pdfjs runtime assets — copied out of node_modules/pdfjs-dist by an inline
# plugin in vite.config.ts on every dev/build (see PdfView.svelte)
/static/pdfjs

# i18n: generated by @inlang/paraglide-js (vite.config.ts) from
# project.inlang/ + messages/*.json — regenerated on every build/dev start
src/lib/paraglide
