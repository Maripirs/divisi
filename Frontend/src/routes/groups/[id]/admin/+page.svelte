<!-- Never rendered — `+page.server.ts`'s load always redirects to
     `/groups/[id]?view=admin`. Kept only because SvelteKit requires a
     +page.svelte alongside +page.server.ts for a page route. -->
