<script lang="ts">
	// `+page.server.ts`'s `load` always redirects before this ever renders —
	// present only because SvelteKit requires a `+page.svelte` alongside
	// any `+page.server.ts` for the route to exist at all.
</script>
