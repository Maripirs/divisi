// F14 spike: OSMD-only, browser-only. Not linked from anywhere in the app —
// reachable at /spike/f14-editor while deciding the editor engine. Delete
// once the decision is recorded in Frontend/plan.md.
export const ssr = false;
export const prerender = false;
